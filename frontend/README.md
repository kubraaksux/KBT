# Full-Stack LLM Chat — Frontend

React Router v7 and TypeScript client for the Full-Stack LLM Chat project.

## Development

```bash
npm install
npm run dev
```

Set the backend URL in `.env`:

```env
BACKEND_URL=http://localhost:8000
```

See the repository-level README for architecture, backend setup, implemented features, and roadmap.
